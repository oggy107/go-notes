module go-notes

go 1.21.0

// we are not using following but we need to if we are using any remote packages in our code

// we can replace dependencies with local package for make it work locally
// replace github.com/wagslane/mystrings v0.0.0 => ../mystrings

// we need to dependencies here
// require (
// 	github.com/wagslane/mystrings v0.0.0
// )